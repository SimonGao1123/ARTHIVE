import type { User } from "../types/user_types";

export default function CreateList({setUser}: {setUser: (user: User | null) => void}) {
    return (
        <div>
            <h1>Create List</h1>
        </div>
    )
}